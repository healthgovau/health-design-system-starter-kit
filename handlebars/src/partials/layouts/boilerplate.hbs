<!doctype html>
<html lang="en">
    <head>
        <title>
            {{#unless title}}
                HDS Boilerplate
            {{/unless}}
        </title>
        
        {{> includes/head }}

    </head>

    <body class="au-grid">
        <div class="au-body">
            {{> includes/nav--skipto }}

            {{> includes/header }}
            
            {{#> nav-block}}
                {{> includes/nav--primary
                        hero-src="https://picsum.photos//1500/500"
                        hero-alt="Hero 1 alt title"
                        links=menu
                }}
            {{/nav-block}}

            {{#> subheader-block}}
                {{!-- Subheader block --}}
            {{/subheader-block}}

            {{!-- Page layout --}}
            <div class="container not-full-width">
                <div class="row">

                    {{!-- Main content pane --}}
                    <main class="col-sm-8 main-content" id="content" role="main">
                        <div class="region region-content">
                            {{#> main-block}}
                                {{!-- Main content goes here. --}}
                            {{/main-block}}
                        </div>
                    </main>

                    {{!-- Supplmentary navigation pane --}}
                    <div class="col-sm-4 au-side-nav--right rs_skip" role="navigation">
                        <div class="region region-sidebar-second">
                            {{#> sidenav-block}}
                                {{!-- Side goes here. --}}
                            {{/sidenav-block}}
                        </div>
                    </div>
                </div>
            </div>
            
            {{> includes/postbody }}
        </div>

        <footer class="au-footer au-footer--dark au-body au-body--dark" role="contentinfo">
            <div class="container">
                {{#> footer-block}}
                    {{> includes/footer }}
                {{/footer-block}}
            </div>
        </footer>

        {{> includes/backtotop }}

        {{#> scripts-block}}
            {{!-- Custom scripts per page can be added. --}}
        {{/scripts-block}}
    
    </body>
</html>