<div class="health-band {{band-class}}">
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <h2>{{ band-title}}</h2>
                {{> includes/loremipsum}}
            </div>
        </div>
    </div>
</div>